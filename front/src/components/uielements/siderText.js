import styled from "styled-components";

const SiderText = styled.div`
  padding: unset;
  font-style: "DM Sans";
  font-weight: 600;
  font-size: 0.82rem;
  line-height: 0.7rem;
`;

export default SiderText;
